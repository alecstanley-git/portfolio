Base surface for every grouped block of content.

```jsx
<Card interactive>
  <h3>Hybrid rocket motor</h3>
  <p>…</p>
</Card>
<Card accent padding="var(--space-8)">…</Card>
```

Cards never stack shadows at rest — depth comes from the hairline border. Only `interactive` cards lift.
