Status marker for project state — uppercase mono with a glowing signal dot.

```jsx
<Badge tone="ok">Complete</Badge>
<Badge tone="warn">In progress</Badge>
<Badge tone="accent" dot={false}>Featured</Badge>
```

One badge per card, top-right. Not for tech stack — that is `Tag`.
