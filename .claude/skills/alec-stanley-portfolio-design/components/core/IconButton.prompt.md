Square icon-only control — social links, toolbar actions, card affordances.

```jsx
<IconButton icon="github" label="GitHub" />
<IconButton icon="linkedin" label="LinkedIn" variant="ghost" />
```

`label` is required. Use `ghost` inside cards and headers; `secondary` when it sits on the page background.
