Restyled native select — used for project filters and enquiry type.

```jsx
<Select label="Discipline" options={[{value:"all",label:"All"},{value:"aero",label:"Aerospace"}]} />
```

Keeps the native menu; only the trigger is styled.
