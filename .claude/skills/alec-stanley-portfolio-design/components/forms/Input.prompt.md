Single-line field for the contact form.

```jsx
<Input label="Email" type="email" placeholder="you@company.com" hint="I reply within a couple of days." />
<Input label="Name" error="Required" />
```

Labels are uppercase mono and light up in the accent colour on focus. Fields are inset (dark well + inner shadow), not raised.
