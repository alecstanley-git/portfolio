Every figure in a project write-up. Without `src` it renders a dashed slot naming the file that belongs there — use this rather than omitting the figure.

```jsx
<MediaFrame src="../../assets/projects/warman/Picture3.png" caption="The final design" />
<MediaFrame file="Picture3.png" caption="The final design" />
```
