Coarse proficiency readout for the skills grid. Segments, not percentages — a bar chart of self-assessment reads as false precision.

```jsx
<SkillMeter label="MATLAB / Simulink" level={5} note="4 yrs" />
<SkillMeter label="ANSYS Fluent" level={3} note="coursework" />
```

Keep `max` at 5. Pair with a `note` so the level is anchored in something real.
