A single quantified fact. Use in rows of 3–4, separated by hairline rules.

```jsx
<StatBlock value="4" label="Year of 5" />
<StatBlock value="12" unit="+" label="Technical projects" tone="accent" />
```

Only one `accent` per row. Never invent numbers — every stat must be real.
